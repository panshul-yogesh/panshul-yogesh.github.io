{
  "title": "Install OO Containerized on-premises",
  "content": "<div class=\"mw-parser-output\"><p>To deploy OO Containerized, run the following command on the control plane node. If you use custom values for resources in the <strong>values.yaml</strong> file, the <code>global.oo.size</code> parameter gets disabled.</p>\n<div class=\"admonition\"><div class=\"admonition-icon admonition-note-icon\"></div><div class=\"admonition-content admonition-note-content\"><div>\n<p>If you're adding certificates at Helm install time using CLI, ensure to remove the <code>caCertificates</code> parameter from the <strong>values.yaml </strong>file.</p>\n</div></div></div>\n<pre>helm install &lt;<em>OO INSTALL NAME&gt;</em> &lt;<em>OO CHART FILE&gt;</em> --namespace &lt;<i>OO NAMESPACE&gt;</i> -f &lt;<em>values.yaml&gt;</em> --set global.oo.size=xx<em> --set-file \"caCertificates.cert_one\\.crt\"=/path/to/cert_one.crt</em></pre>\n<p>Where:</p>\n<ul>\n<li><code>&lt;OO INSTALL NAME&gt;</code><em> =</em> the unique release name you need to specify for the OO deployment. You need to use the same release name as specified here when you upgrade OO in the future.</li>\n<li><code>&lt;OO CHART FILE&gt;</code><em> = </em>the absolute path of the OO chart file</li>\n<li><code>&lt;OO NAMESPACE&gt;</code><i> = </i>unique namespace of the OO deployment. Use the namespace you specified in the <a href=\"/doc/423/26.1/ooccreateoodeployment\" title=\"Create a new deployment for OO Containerized\">Create a new deployment for OO</a> topic.</li>\n<li><code>&lt;values.yaml&gt;</code> = the path to OO <strong>v</strong><b>alues.yaml </b>file configured as part of the preparation step <a href=\"/doc/423/26.1/399-oocpreparecreateyaml\" title=\"Create values.yaml for OO Containerized on-premises\">Create and configure values.yaml file</a>.</li>\n<li><code>&lt;global.oo.size&gt;</code> =<em> </em>the number of tenants in the OO deployment. Value is between 1 and 20. Default value is 5, if you don't configure this parameter.</li>\n<li><code>&lt;--set-file&gt;</code> = These are optional arguments because they address one way of having OO trust-specific certificates. Depending on your choice during the preparation of OO <strong>values.yaml </strong>(see <a href=\"/doc/oo_containerized/main/oocpreparecreateyaml#SMAX_integration_parameters\" title=\"SMAX integration parameters\">Service Management integration parameters</a>):<br/>\n\tIf you opted for providing the required certificates at OO install time, then you must do so using the <code>--set-file</code> parameter.<br/>\n\tIf you provided the certificates directly in the OO <strong>values.yaml</strong>, then don't use the <code>--set-file</code> parameter with the Helm install command.</li>\n</ul>\n<p>For example:</p>\n<pre>helm install oo oo-helm-charts-1.x.x+26.x.x/oo-helm-charts/charts/oo-1.x.x+26.x.x.tgz --namespace oo -f values.yaml --set global.oo.size=5 --set-file \"caCertificates.cert_one\\.crt\"=/path/to/cert_one.crt</pre>\n<p class=\"Admonition_Note\">You can run the command mentioned earlier with the <code>--dry-run</code> argument to simulate an installation. It's useful to validate the content in the <strong>values.yaml</strong> file before a real installation.</p>\n</div>",
  "modifiedon": "2025-11-21 07:15:40"
}