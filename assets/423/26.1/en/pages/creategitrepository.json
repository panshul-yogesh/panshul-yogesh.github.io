{
  "title": "Create a Git repository",
  "content": "<div class=\"mw-parser-output\"><p class=\"mw-empty-elt\">\n</p><p>If you don't have a Git server account, you can create a local Git repository. Operations Orchestration (OO) Workflow Designer includes a batch file for creating this repository: <code>git-create-repo.bat</code>.</p>\n<ol>\n<li>Double-click the <b>git-create-repo.bat</b> file in the <code>&lt;installation path&gt;/bin</code> folder.</li>\n<li>In the command line dialog box, enter the path to an existing folder on the current machine or on a network location, where you want to create the repository. For example: <code>\\\\xxx.net\\shared\\GIT\\GitRepo</code>. If the repository is on a shared file system, make sure that the user for whom the OO Workflow Designer server will run has read and write permission for this file system.</li>\n<li>\n<p>You are now prompted to enter a name for the new repository to create under the path you provided earlier.</p>\n<p>Add any name. You don't need to add the suffix \"<code>.git</code>\" to the name, as its added by the tool.</p>\n<p>The repository name shouldn't contain characters that are forbidden by Windows or Linux, for example, <code>*, &lt;, &gt;, or %</code> and shouldn't begin and end with white spaces.</p>\n</li>\n<li>\n<p>The URL of the newly created Git repository displays. Make a note of this URL, and press any key to close the command line dialog box.</p>\n<p>The URL should be in the following format: f<code>ile://&lt;path&gt;/&lt;repo&gt;.git</code></p>\n</li>\n</ol>\n<p class=\"mw-empty-elt\"></p></div>",
  "modifiedon": "2025-10-24 08:51:12"
}