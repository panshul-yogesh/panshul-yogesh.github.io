{
  "title": "DND service consumes extra CPU cores causing higher utilization on worker node",
  "content": "<div class=\"mw-parser-output\"><p class=\"mw-empty-elt\"></p><div class=\"mw-parser-output\"><p class=\"mw-empty-elt\">\n</p><p>While using the solution in Medium deployment size, under high load, the DND service consumes two additional CPU cores, and that results in higher CPU utilization on the worker node.</p>\n<h2>Cause</h2>\n<p>This issue occurs because of the high throughput on the system requiring more CPU resources in DND service pod.</p>\n<h2>Solution</h2>\n<p>Add two extra CPU cores to the worker node in which DND is running.</p>\n<p class=\"mw-empty-elt\"></p></div>\n<p class=\"mw-empty-elt\"></p></div>",
  "modifiedon": "2025-10-24 08:51:12"
}