{
  "title": "Perform pre-installation checks",
  "content": "<div class=\"mw-parser-output\"><p class=\"mw-empty-elt\">\n</p><p>This topic contains steps needed to check if your environment is ready for the new installation. Perform the steps listed here before starting your installation.</p>\n<h2>Check the status of database</h2>\n<p>Use the <code>checkPG</code>.sh script included in the ESM helm chart to check the status of your database server:</p>\n<ul>\n<li>If you want to use the bastion node or a control plane node to do this, make sure the host has a PostgreSQL client installed, and then navigate to the following directory: &lt;ESM_Helm_Chart-2x.x&gt;/scripts/precheck.</li>\n<li>If you want to use the PostgreSQL server to do this, you need to copy the script &lt;ESM_Helm_Chart-2x.x&gt;/scripts/precheck/checkPG.sh from the bastion node or a control plane node to the PostgreSQL server. </li>\n</ul>\n<p>Then, run the following commands on the bastion node, a control plane node, or the PostgreSQL server:</p>\n<pre>chmod u+x checkPG.sh\n./checkPG.sh -h &lt;pgserver&gt; -p &lt;port&gt; -u &lt;dbUserName&gt;</pre>\n<p>Example:</p>\n<pre> ./checkPG.sh -h example.test.pgserver -p 5432 -u postgres </pre>\n<p>If pg_trgm extension does not exist, refer to <a href=\"/doc/423/26.1/prepareexternaldbs#General_requirements_for_PostgreSQL_databases\" title=\"General requirements for PostgreSQL databases\">General requirements for PostgreSQL databases</a> to install it. </p>\n<h2>Check Vertica (deployment with FinOPs Only)</h2>\n<p>Perform this task only when you are deploying the CMP FinOps capability.</p>\n<p>Log in to the Vertica server and run the command below to check if Vertica can be connected successfully.</p>\n<pre>&lt;vsql_client_path&gt;/vsql -h $HOST -p $PORT -U $USER -w $PASSWORD -d $DATABASE </pre>\n<p>Example:</p>\n<pre> /opt/vertica/bin/vsql -h example.test.verticaServer -p 5433 -U dbUser -d dbName </pre>\n<h2>Check NFS (embedded K8s deployment only)</h2>\n<p>Log in to the control plane node and run the following command to manually check the NFS status:</p>\n<pre> $K8S_HOME/scripts/checkNFS.sh -t nfs -s &lt;nfs-server&gt; -f &lt;nfs-base-path&gt;</pre>\n<p>For example:</p>\n<pre>$K8S_HOME/scripts/checkNFS.sh -t nfs -s example.nfs.server -f /var/vols/itom/itsma</pre>\n<p>If the NFS server status is OK, your terminal should display the following message:</p>\n<pre>Checking server and shared folder: passed\n</pre>\n<p class=\"mw-empty-elt\"></p></div>",
  "modifiedon": "2025-10-24 08:51:12"
}