{
  "title": "UCMDB CIs cannot be synchronized to Service Management through Native SACM",
  "content": "<div class=\"mw-parser-output\"><p class=\"mw-empty-elt\"></p><div class=\"mw-parser-output\"><p class=\"mw-empty-elt\">\n</p><p>With Native SACM enabled, Service Management CIs can be synchronized to UCMDB successfully, however, UCMDB CIs can't be synchronized to Service Management.</p>\n<h2>Cause</h2>\n<p>This issue may be caused by custom business rules that restrict the write permission on SACM fields.</p>\n<h2>Solution</h2>\n<p>To identify and fix this issue, you can use the following method:</p>\n<ol>\n<li>\n<p>Update a UCMDB CI and check the cms-gateway logs (<code>Error.log</code>/<code>server.log</code>/<code>subscription.log</code>), if you can find the corresponding update in <code>subscription.log</code> and see no errors in other logs, you can rule out the UCMDB Gateway malfunction. Otherwise, examine the UCMDB settings.</p>\n</li>\n<li>\n<p>Check the Service Management log files (<code>maas_cms-x.log</code>/<code>maas_unhandled_exceptions.log</code>/<code>maas_error.log</code>), search for the update with the global ID, and check if there is an error. </p>\n</li>\n<li>\n<p>If the logs show no error, examine custom business rules. Pay attention to the following rules:</p>\n<ul>\n<li>\n<p>Define fields as read-only</p>\n</li>\n<li>\n<p>Define fields as mandatory</p>\n</li>\n<li>\n<p>Restrict editing of field</p>\n</li>\n</ul>\n</li>\n<li>\n<p>If you have rules that restrict the white permission on SACM fields, try to disable them and see if the issue disappears.</p>\n</li>\n</ol>\n<p class=\"mw-empty-elt\"></p></div>\n<p class=\"mw-empty-elt\"></p></div>",
  "modifiedon": "2025-10-24 08:51:12"
}